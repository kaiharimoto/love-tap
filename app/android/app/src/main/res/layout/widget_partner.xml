<?xml version="1.0" encoding="utf-8"?>
<!-- The other person, on the home screen. The colours are the desk's own (DIRECTION.md), the two
     lines are their name and the one sentence, and there is nothing else on it. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_who"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#F1ECDF"
        android:textSize="17sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:letterSpacing="0.04" />

    <TextView
        android:id="@+id/widget_line"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="#CFC4B2"
        android:textSize="14sp"
        android:maxLines="3"
        android:ellipsize="end" />
</LinearLayout>
